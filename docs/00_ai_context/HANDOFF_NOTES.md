---
owner: product
status: active
last_updated: 2026-04-11
source_of_truth: true
---

# Handoff notes

## What changed

- **Phase 3 — hub content pipeline + universal content loading (2026-04-11):** **HubPageTemplate** bodyHtml wiring; all hub + guide routes content-wired. 3 hub pages (newcomer, documents, housing). **.hub-body** CSS. Any new Markdown file renders automatically on matching route.
- **Phase 3 — guide content pipeline + address-registration (2026-04-11):** **renderMarkdownToHtml** (unified + remark + rehype, sync). **loadPageContent** returns PageContent | null. **GuidePageTemplate** bodyHtml prop (content or placeholder fallback). First real guide: **/documents/address-registration** with structured Markdown sections + trust blocks. **.guide-body** CSS. **Phase 3 transition:** CURRENT_PHASE.md updated.
- **Phase 2 — stay calculator page wiring (2026-04-11):** **StayCalculatorBlock.tsx** ("use client"): add/remove stay entries + **calculateStay** → result (days/status/window/warning). **CalculatorPageTemplate** children pattern. **/documents/stay-calculator** renders interactive calculator. **stay-calc*** CSS. 5 Vitest cases (filterValidEntries helper). **Phase 2 exit criteria met: onboarding → dashboard (NBA + checklist) → guide trust blocks → stay calculator page.** No i18n, no guest persistence, no dashboard residency card.
- **Phase 2 — stay calculator logic v1 (2026-04-11):** **stay-calculator.ts** + 12 Vitest cases — pure **calculateStay**: 90-in-180 rolling window (Set-based unique day count, overlapping entry safe, clamp to window/asOf), **StayCalculatorResult** with daysUsed/daysRemaining/status (safe >20, watch >7, urgent ≤7). **Module-only — no UI, no page wiring, no dashboard.**
- **Phase 2 — guide trust wiring (2026-04-11):** **loadTrustDataForPage** in **load-trust-for-page.ts** + 5 Vitest cases: sources by **page_id** + page **last_verified_at** / **what_may_vary**. **GuidePageTemplate** optional trust props → **SourceBlock** + **LastVerifiedNote** + **WhatMayVaryNote** (placeholder fallback for unseeded pages). **documents/[slug]/page.tsx** server-loads trust data. 2 seed sources + 1 seed page for address-registration. **First guide with real trust blocks. No i18n, no other routes.**
- **Phase 2 — trust UI components (2026-04-11):** **SourceBlock.tsx** (SourceRecordFrontmatter[] → label + type + confidence + link + notes), **LastVerifiedNote.tsx** (date string), **WhatMayVaryNote.tsx** (variance note, low/high variant) — three presentational components per UI_HANDOFF_SPEC §3.5 / §8.2 Blocks 7–8 / §11 Source Block states. **fg-source-block***, **fg-last-verified***, **fg-what-may-vary*** BEM classes. 10 Vitest cases. **Presentational only — no guide page wiring, no i18n, no source seeding.**
- **Phase 2 — dashboard checklist block (2026-04-11):** **`src/features/dashboard/DashboardChecklistBlock.tsx`** ("use client") wires checklist chain to **`/dashboard`**: **`page.tsx`** server-loads **`ChecklistItemFrontmatter[]`** via **`loadValidatedChecklistItems`** → client reads guest blob → **`filterChecklistItems`** → **`groupChecklistByCategory`** → **`ChecklistItemRow`** per row, grouped by category. **`dashboard-checklist*`** CSS. 7 Vitest cases (pure grouping helper). **First visible checklist on /dashboard. No i18n, no UserChecklistStatus, no trust/alerts/stay-calc.**
- **Phase 2 — checklist row component (2026-04-11):** **`src/components/ui/ChecklistItemRow.tsx`** + **`ChecklistItemRow.test.ts`** — presentational component for one **`FilteredChecklistItemV1`** row: title, category, urgency marker, status chip, effort, destination link; **`fg-checklist-row*`** BEM classes in **`globals.css`**; **`prerequisites_met === false`** → visual lock. **Presentational only — no dashboard wiring, no i18n, no state management.** Vitest 10 cases.
- **Phase 2 — seed checklist items v1 (2026-04-11):** 8 new **`src/content/checklist-items/*.md`** seed rows (SIM, address registration, social card, stay days, housing, bank account, terminals, quick income) — Zod-valid frontmatter + **`applies_to_rules`** / **`prerequisite_ids`** per **`DATA_CONTENT_MODEL_SPEC.md`** §7; **`loadValidatedChecklistItems`** loads 9 files including **`sample-row.md`**. Vitest **`load-checklist-and-updates.test.ts`** asserts **`checklists.length >= 9`**. **Seed content fixtures only — no code changes, no dashboard wiring, no UI rendering.** (Exception: one assertion line in that test file.)
- **Phase 2 — checklist filtering logic v1 (2026-04-11):** **`src/lib/checklist/checklist-filter.ts`** + **`checklist-filter.test.ts`** — pure **`parseChecklistRule`** / **`evaluateChecklistRule`** / **`filterChecklistItems`** on **`ChecklistItemFrontmatter`** + **`GuestOnboardingPartial`**: active → **`applies_to_rules`** (AND, fail-soft parse; inclusive when field undefined or **`guestState`** undefined) → urgency + **`default_order`** sort → **`prerequisites_met`** vs filtered id set (items not removed for unmet prereqs). **Module-only — no dashboard UI wiring, no checklist row UI, no visible checklist rendering on `/dashboard`.** No blob/schema/persist/routes/trust/auth/i18n changes.
- **Phase 2 — Dashboard NBA v1 consumer (2026-04-06):** **`/dashboard`** — **`DashboardNextBestAction.tsx`** (client) replaces **`RoutePlaceholder`**: **`readGuestBlob`** + **`deriveGuestOnboardingOutcomePreviewV1(blob?.onboarding)`**; states — loading (no CTA), no blob (primary **`/start`**, secondary **`/`** via **`shell.start` / `shell.home`**), incomplete onboarding (same CTAs, distinct copy), valid preview (NBA block: **`dashboard.nba.sectionTitle`**, **`onboardingStart.step6.emphasis.*`**, primary + up to 2 secondaries via **`step6SecondaryVisibleLabel`**). **`messages/en|fa|ru.json`** **`dashboard.*`** (parity). **`globals.css`** **`.dashboard-page__title`**. **No** full dashboard shell, checklist/trust/residency/alerts, auth/save-path, DTO/blob/schema/persistence edits, redirects.
- **Phase 2 onboarding Step 6 v1 — Result Summary UI (2026-04-05):** **`/start`** end state (`step === "end"`) in **`StartOnboardingFlow.tsx`** consumes **`deriveGuestOnboardingOutcomePreviewV1(blob?.onboarding)`** — title/intro, localized **`emphasis`** line, primary link from **`reason_key`** + **`page_slug`**, optional secondaries from DTO with slug→i18n map in **`onboarding-step6-labels.ts`** + **`onboarding-step6-labels.test.ts`**. **Happy-path CTAs:** primary **`/dashboard`**, secondary **`/`** (continue as guest). **`preview === null` fallback:** error title/body + CTAs **`/`** then **`/dashboard`**. **`messages/en|fa|ru.json`** **`onboardingStart.step6.*`** (parity). **`globals.css`** Step 6 layout helpers. **No** dashboard product implementation, auth/save-path, DTO/blob/signals changes, NBA/checklist/trust.
- **Phase 2 onboarding outcome preview v1 — pure DTO (2026-04-04):** **`GuestOnboardingOutcomePreviewV1`** + related types + **`deriveGuestOnboardingOutcomePreviewV1`** in **`src/lib/onboarding/onboarding-outcome-preview.ts`**; Vitest **`onboarding-outcome-preview.test.ts`**. **`null`** unless **`resolveStartSlicePhase(onboarding).kind === "end"`**; else **`emphasis`** from **`deriveGuestOnboardingSignalsV2`** only; **R1** = §15 example (`iran` + `inside_armenia` + `has_housing` + ¬`has_address_registration` + ¬`has_social_card`) → primary + two secondaries per spec; else **`fallback_by_emphasis_v1`** hub table + empty secondaries. **No** UI, routes, blob schema, persist, content loaders, dashboard, Step 6, full NBA, checklist filter.
- **Phase 2 onboarding signals v2 — emphasis with `has_*` (2026-04-04):** **`GuestOnboardingSignalsV2`** + **`deriveGuestOnboardingSignalsV2`** in **`src/lib/onboarding/onboarding-emphasis.ts`**; Vitest coverage in **`onboarding-emphasis.test.ts`**. **`deriveGuestOnboardingEmphasisV1`** unchanged. When core + all five **`has_*`** are complete, **R1** (Iran + inside Armenia + `has_housing` true + `has_address_registration` false + `has_social_card` false per **`DATA_CONTENT_MODEL_SPEC.md`** §15 example) forces **`emphasis: "documents"`**; else **`emphasis`** equals v1. Uses **`STEP5_HAS_FIELD_ORDER`** from **`start-slice.ts`** for Step 5 completeness. **No** UI, routes, blob schema, persist, dashboard/NBA/checklist/trust.
- **Phase 2 onboarding Step 5 v1 (2026-04-04):** After shipped steps 1–4, **`/start`** adds **one screen** with five **`has_*`** rows (`has_housing`, `has_sim`, `has_address_registration`, `has_social_card`, `has_bank_account`) — each row **Yes/No** → explicit **`true`/`false`**; **Next** disabled until all five answered; **no** partial persist of Step 5; on **Next** from Step 5, all five booleans + four core fields written to **`GuestBlobV1.onboarding`** via **`mergeOnboardingAfterStep5`**; **`resolveStartSlicePhase`** → Step 5 if core complete but any **`has_*`** missing; **end** only when all nine fields set; **re-Next from Step 4** prunes all **`has_*`**. Progress **۵** segments; completion transitions to end state (**Step 6 UI** ships separately — see Step 6 bullet). **`src/lib/onboarding/start-slice.ts`** (`STEP5_HAS_FIELD_ORDER`, helpers); **`start-slice.test.ts`**; **`StartOnboardingFlow.tsx`**; **`globals.css`** Step 5 layout; **`messages/en|fa|ru.json`** `onboardingStart.step5Title` + `options.step5.*`. **`deriveGuestOnboardingEmphasisV1`** unchanged.
- **Cursor Cognee MCP (dev machine, 2026-04-04):** User **`~/.cursor/mcp.json`** **`cognee`** entry was pointing at a **missing** `fix-cognee-mcp.ps1` and **`powershell` without `-NoLogo`** was polluting **stdio** with the PowerShell banner (invalid JSON for MCP). **Fix:** **`cognee`** now uses **`"url": "http://localhost:8000/mcp"`** per [Cognee Cursor docs](https://docs.cognee.ai/cognee-mcp/integrations/cursor). **`ECONNREFUSED` on :8000** means **Docker Desktop is not running** and/or the **Cognee container is not started** — helper **`scripts/start-cognee-mcp-docker.ps1`** (expects **`%USERPROFILE%\.cursor\cognee-mcp.env`** with **`LLM_API_KEY`**; use **`-Detach`** for background). Optional **stdio** launcher: **`scripts/run-cognee-mcp-stdio.ps1`** + **`COGNEE_MCP_ROOT`** + **`uv`** (see comments in that script).
- **Phase 2 onboarding emphasis derivation v1 (2026-04-04):** Pure **`deriveGuestOnboardingEmphasisV1`** + type **`GuestOnboardingEmphasisV1`** in **`src/lib/onboarding/onboarding-emphasis.ts`**; Vitest **`onboarding-emphasis.test.ts`**. Reads only **`GuestOnboardingPartial`** fields `language`, `nationality`, `location_status`, `primary_goal`; ignores **`has_*`** in v1; missing any of the four → **`neutral`**; **`inside_armenia`** maps **`primary_goal`** to emphasis; **`outside_armenia`** + **`start-life`** → **`life-setup`**, else **`documents`/`housing`/`work`** → **`neutral`**. **No** UI, routes, blob schema, persist, dashboard/NBA/checklist/trust.
- **Phase 2 onboarding `/start` slice v1 (initial 2026-04-04):** Original ship: steps 1–4 only + minimal end after four fields. **Superseded for completion path** by **Step 5 v1** bullet above (nine-field end؛ progress ۵؛ همان فایل‌ها + `mergeOnboardingAfterStep5`).
- **Phase 2 guest persistence v1 (module-only, 2026-04-04):** **`src/lib/guest/constants.ts`**, **`persistence.ts`**, barrel **`storage.ts`** — `localStorage` key **`flowguide_guest_v1`**, envelope **`schemaVersion`**, **`guestSessionId`** (UUID), **`createdAt`** / **`lastActiveAt`** (ISO-8601), optional **`onboarding`** per **`DATA_CONTENT_MODEL_SPEC.md`** §15 with enums from **`user.ts`**. Zod **`src/lib/schemas/guest-blob.ts`**; sliding **90-day** TTL from **`lastActiveAt`**; invalid JSON / envelope / expiry / mismatch → **clear** + **`null`**; successful **read** touches **`lastActiveAt`** and persists. APIs: **`readGuestBlob`**, **`readGuestBlobFromStorage`**, **`writeGuestBlob`**, **`writeGuestBlobToStorage`**, **`clearGuestBlob`**, **`clearGuestBlobInStorage`**, **`createInitialGuestBlob`**. Vitest **`guest-blob.test.ts`**, **`persistence.test.ts`**. **Not in scope:** any route or UI, onboarding wizard, dashboard, NBA, checklist persistence, trust, auth.
- **Phase 1 exit gate + formal closure (2026-04-04):** Audited repo vs historical Phase 1 exit list ([`CURRENT_PHASE.md`](CURRENT_PHASE.md) now describes **Phase 2**; criteria summarized in [`ROADMAP_STATUS.md`](../01_strategy/ROADMAP_STATUS.md) / [`DEVELOPMENT_BREAKDOWN.md`](../05_execution/DEVELOPMENT_BREAKDOWN.md) Gate A): **shell + IA route skeleton** (`src/middleware.ts` airport redirect, `ia-phase1-routes` + tests); **Zod toward** [`DATA_CONTENT_MODEL_SPEC.md`](../02_product/DATA_CONTENT_MODEL_SPEC.md) (content page, checklist, update, source, place, user/domain stubs); **placeholder templates** hub/guide/calculator/utility/service-form (`page-type-templates.tsx`, `page-type-routes`); **`/`** / **`/start`** / **`/dashboard`** = `RoutePlaceholder` only; **git Markdown + prebuild** `search-index.json` (`package.json` `prebuild`, `scripts/build-search-index.mjs`); **`.env.example`** + README; **`npm run lint` / `test` / `build`** + PR CI; **Plausible + Sentry** env-gated stubs + **`logger.ts`** instrumentation `logInfo`; **no** NBA/checklist/guest product logic beyond stubs/loaders. **Regression:** all green. **Docs:** Phase 1 **done**, Phase 2 **in progress** in status/context. **No application code changes** in this slice.
- **Phase 1 Epic 1.3 domain Zod stubs (2026-04-04):** **`user.ts`**, **`user-state.ts`**, **`user-checklist-status.ts`**, **`request-submission.ts`** under **`src/lib/schemas/`** per **`DATA_CONTENT_MODEL_SPEC.md`** §5–6, §8, §13 (verbatim enum tokens). Focused Vitest for valid/invalid enum parses and minimal record/stub shapes. **Not in scope:** `localStorage` guest blob, Route Handlers, trust/search wiring, Phase 1 exit marking.
- **Phase 1 Place content contract (2026-04-04):** Zod **`place.ts`** (**`DATA_CONTENT_MODEL_SPEC.md`** §12); **`parseMarkdownPlace`** in **`parse-md.ts`**; fail-fast **`loadValidatedPlaces`** in **`load-places.ts`**; **`src/content/places/`** + sample fixture; Vitest **`place.test.ts`**, **`load-places.test.ts`**. **`confidence_level`** reuses **`sourceConfidenceLevelSchema`**. Canonical frontmatter key **`place_type`** (§21 `type` = same concept, not a second YAML key in Phase 1). **Not in scope:** search-index rows, `/places` routes, map UI, page linkage, Phase 1 exit marking.
- **Phase 1 Source Record content contract (2026-04-04):** Zod **`source-record.ts`** (**`DATA_CONTENT_MODEL_SPEC.md`** §10); **`parseMarkdownSourceRecord`** in **`parse-md.ts`**; fail-fast **`loadValidatedSourceRecords`** in **`load-sources.ts`**; **`src/content/sources/`** + sample fixture; Vitest **`source-record.test.ts`**, **`load-sources.test.ts`**. **`confidence_level`** uses **`sourceConfidenceLevelSchema`** (`high` / `medium` / `low` only), not **`urgencyTagSchema`**. **Not in scope:** search-index, trust UI, page consumption of sources, Phase 1 exit marking.
- **Phase 1 checklist + update content contracts (2026-04-04):** Zod **`checklist-item.ts`** / **`update-item.ts`** (aligned to **`DATA_CONTENT_MODEL_SPEC.md`** §7 / §11); **`parseMarkdownChecklistItem`** / **`parseMarkdownUpdateItem`** in **`parse-md.ts`**; Node **`load-checklist-and-updates.ts`** loaders (fail-fast). Canonical **`src/content/checklist-items/`** and **`src/content/updates/`** with sample fixtures; Vitest for schemas + loaders. **`urgencyTagSchema`** reused for **`urgency_level`** / **`impact_level`**. **Not in scope:** checklist filtering, dashboard, guest persistence, `/updates` product UI, search-index ingestion, RequestSubmission handlers/storage, Phase 1 exit marking.
- **Phase 1 shared UI baseline (2026-04-04):** **`globals.css`** — consolidated spacing/radius/typography tokens and **`--surface-muted`**; **`.fg-button`**, **`.fg-card`**, **`.fg-section-header`** for thin primitives. **`src/components/ui/Button.tsx`**, **`Card.tsx`**, **`SectionHeader.tsx`**; **`SiteHeaderChrome`** hamburger uses **`Button`**; **`LocaleSwitcher`** locale submits use **`Button`** (same server-action behavior). **`page-type-templates.tsx`** — **`Card`** + **`SectionHeader`** for template blocks and utility grouped placeholder. **`src/components/ui/primitives.test.ts`**; **`vitest.config.ts`** esbuild **`jsx: automatic`**. **Not touched:** **`RoutePageBanner`**, **`RoutePlaceholder`**, **`/`** / **`/start`** / **`/dashboard`** product structure.
- **Responsive app shell + mobile navigation (2026-04-04, Phase 1 slice):** **`SiteHeaderChrome`** — client drawer for the existing IA link list below **48rem**; **`LocaleSwitcher`** remains in the header row on mobile; desktop keeps inline nav via CSS **`display: contents`** on the top row. **`globals.css`** adds drawer/backdrop, **`messages/*`** adds **`shell.primaryNav`**, **`menuOpen`**, **`menuClose`**. Closes on Escape, backdrop click, and navigation. **No** product logic; **`/start`** / **`/dashboard`** unchanged except shared header layout.
- **Phase 1 page template shells (2026-04-03):** **`src/features/routes/page-type-templates.tsx`** — hub, guide, calculator, utility, service-form, and housing-request-success **structural** placeholders (i18n **`pageTemplate`**); **`RoutePageBanner`** primitive; **`RoutePlaceholder`** composes banner for non-IA slice routes only. Dynamic routes: **`documents/[slug]`** uses calculator template for **`stay-calculator`**; **`housing/[slug]`** uses service-form template for **`request`**. Tests: **`src/lib/page-type-routes.test.ts`**. **`/start`** and **`/dashboard`** untouched.
- **Prompt authoring guidance hardening (2026-04-03):** Added **`PROMPT_AUTHORING_PROTOCOL.md`** with concrete templates for **next-task selection**, **candidate-task evaluation**, **plan refinement**, and **implementation after approval**. Updated **`CURSOR_NEW_CHAT_PROTOCOL.md`**, **`CURSOR_PLANNING_PROTOCOL.md`**, **`AI_INDEX.md`**, **`AI_WORKING_RULES.md`**, and **`.cursor/rules/project-context.mdc`** so future ChatGPT/Cursor sessions stay **discovery-first**, validate **canonical naming**, separate **exit-criteria gaps** from **cleanup** and **deferred work**, avoid **defense-first** prompts, and use deterministic **"Read these files in order"** wording. No product scope, roadmap substance, or app code changed.
- **Content frontmatter schema alignment (2026-04-03, Phase 1 slice):** **`src/lib/schemas/content-page.ts`** — bounded optional fields toward **`DATA_CONTENT_MODEL_SPEC.md`** §9: **`intent_type`**, **`related_page_slugs`**, **`searchable`**, **`dashboard_linkable`**, **`map_linked`**, **`urgency_tag`**, **`published_at`**, **`updated_at`**, **`is_active`** (all optional; existing Markdown unchanged). **`shouldIncludeInSearchIndex`** — **`build-search-index-records`** skips pages/FAQ when **`searchable: false`** or **`is_active: false`**. Vitest extended in **`content-page.test.ts`** and **`build-search-index-records.test.ts`**. Sample **`welcome.md`** sets **`intent_type: start`**. **Not in scope:** Source records, block body, trust UI, dashboard logic, Phase 1 exit marking.
- **IA Phase 1 route contract + regression guard (2026-04-04, Phase 1 slice):** **`src/lib/ia-phase1-routes.ts`** — canonical **`PHASE1_IA_PAGE_PATHS`** (32 paths) from **`IA_SPEC.md`** §6.1–6.9 only; **`PHASE1_IA_AIRPORT_REDIRECT`** for **`/transport/airport` → `/newcomer/airport-to-city`**. **`src/middleware.ts`** imports the redirect constant (single source of truth). **`src/lib/ia-phase1-routes.test.ts`** asserts each page path resolves to an existing **`src/app/[locale]/…`** shell (static, **`[slug]`**, or **`housing/request/success`**), **`ROUTE_TITLES`** coverage, **no** `transport/airport/page.tsx`, and middleware wiring. **Out of scope:** `/places/*`, grouped search, nav redesign, Phase 1 exit marking.
- **Structured logging baseline (2026-04-04, Phase 1 slice):** **`src/lib/observability/logger.ts`** — one **JSON object per line** (`level`, `message`, `timestamp`, `service`, `env`); **`src/lib/observability/logger.test.ts`** (Vitest shape checks). **`src/instrumentation.ts`** calls **`logInfo("instrumentation_register_complete")`** once after Sentry server/edge config load — **no** request logging, **no** PII. **Not included:** log platform, source maps, analytics events, Phase 1 exit marking.
- **IA housing route parity (2026-04-03, Phase 1 slice):** **`IA_SPEC.md`** §6.5 shells for **`/housing/request`** (slug **`request`** in **`HOUSING_SLUGS`**) and **`/housing/request/success`** (dedicated **`page.tsx`**). **`ROUTE_TITLES`** entries: “Housing request” / “Housing request follow-up” (neutral—no submission-success copy). No forms or Phase 2 logic.
- **Observability hardening (2026-04-03, Phase 1 slice):** Added **`@sentry/nextjs`** with root **`sentry.client.config.ts`**, **`sentry.server.config.ts`**, **`sentry.edge.config.ts`** — **`Sentry.init` only when DSN env vars are set**; **`src/instrumentation.ts`** loads server/edge configs and exports **`onRequestError`**. **Plausible** does not load in **`next dev`** unless **`NEXT_PUBLIC_PLAUSIBLE_ENABLE_DEV=true`**. Removed unused **`src/lib/observability/sentry-stub.ts`**. **No** Phase 1 exit / roadmap closure, **no** product analytics events at that slice. See **`ENGINEERING_ARCHITECTURE.md`** §9, **`.env.example`**, **`README.md`**. (Structured logging baseline: **2026-04-04** bullet above.)
- **AI planning protocol (2026-04-03):** Added **`CURSOR_PLANNING_PROTOCOL.md`** — deterministic planning workflow (phase-strict, 2–4 candidates, exit-criteria vs nice-to-have labels, smallest slice, execution-ready output). Wired from **`CURSOR_NEW_CHAT_PROTOCOL.md`**, **`AI_INDEX.md`**, **`AI_WORKING_RULES.md`**, **`UPDATE_PROTOCOL.md`** (new “AI / planning guidance changes” section), and **`.cursor/rules/project-context.mdc`**. No product, roadmap substance, or app code changes.
- **Phase 1 PR CI (2026-04-02):** **`.github/workflows/ci.yml`** now runs **`npm run lint`** → **`npm run test`** → **`npm run build`** after **`npm ci`** (**`pull_request`** to **`main`**, Node **20**, npm cache, **`cancel-in-progress`** unchanged). Phase 1 quality gate only; no deploy/Docker/platform expansion.
- **`DEVELOPMENT_BREAKDOWN.md` (2026-04-02):** Aligned with **`ROADMAP_MASTER`** / **`CURRENT_PHASE`**: canonical phase table + Phase 1 shipped vs remaining; epic headers tagged; §4 marked **legacy** (naming clash called out); §13 gate note; §14 “must-launch ≠ Phase 1”; §18 = Phase 5; §19 **phase gates A–E** + deprecated sprint archive; Ticket **2.2** locale line updated; Epic **7** search split (Phase 1 index vs Phase 4 UX).
- **Phase 1 PR CI (initial slice, 2026-04-02):** First **`.github/workflows/ci.yml`** — **`pull_request`** to **`main`**, Node **20**, **`npm ci`**, **`npm run lint`**, **`npm run build`**, concurrency **`cancel-in-progress`**. (**Vitest** step added same day — see bullet above.)
- **Phase 1 next-intl route repair (2026-04-01):** Moved all route **`page.tsx`** trees under **`src/app/[locale]/`** + thin **`[locale]/layout.tsx`** (`setRequestLocale`, `generateStaticParams`, invalid locale → `notFound`). Fixes **404** on hub routes when middleware rewrites to **`/en/...`** with **`localePrefix: "never"`**. Root **`src/app/layout.tsx`** unchanged (shell + `getLocale` / `getMessages`). **`/transport/airport`** redirect unchanged.
- **Phase 1 i18n (2026-04-01):** Hardened **`src/i18n/request.ts`** (`requestLocale` + locale validation + JSON fallback). Added **`LocaleSwitcher`** in shell + **`setLocaleAction`** (`NEXT_LOCALE` cookie, redirect to current pathname). **`messages/*`** keys aligned; **`src/i18n/messages-parity.test.ts`**. **`?lang=`** not implemented—defer to a later slice if still wanted per `ENGINEERING_ARCHITECTURE` §7.
- **Phase 0 approved** (2026-04-01): [`PHASE_0_DECISION_RECORD.md`](../04_engineering/PHASE_0_DECISION_RECORD.md) `status: approved`, `source_of_truth: true`, new **§0 Approval** summary.
- **Roadmap / context** (2026-04-01 snapshot): [`ROADMAP_STATUS.md`](../01_strategy/ROADMAP_STATUS.md) marked **0A/0B done** and Phase 1 scaffold underway; superseded by **2026-04-04** Phase 1 exit closure (see top bullet).
- **Phase 1 scaffold:** Next.js App Router + TypeScript at repo root — `src/app/` route skeleton (IA list + airport redirect), `src/lib/`, `src/content/`, Plausible/Sentry **stubs**, **no** Phase 2 product logic, **no** auth/CMS/Docker.
- **Phase 1 content plumbing (2026-04-01):** Shared **Zod** validation for `src/content/**/*.md`; **`build-search-index.mjs`** delegates to **`build-search-index.impl.ts`** via **`tsx`**; output **`search-index.json`** validated before write. Cursor **rule** `../../.cursor/rules/docs-update-policy.mdc` (**alwaysApply**); project **skill** `../../.cursor/skills/flow-guide-update-protocol/SKILL.md` (doc routing after meaningful changes).

## Repository reality

- **Phase 1 is complete (2026-04-04)** — exit audit + **`npm run lint` / `test` / `build`** passed; **Phase 2** is the active implementation track ([`CURRENT_PHASE.md`](CURRENT_PHASE.md), [`ROADMAP_STATUS.md`](../01_strategy/ROADMAP_STATUS.md)). **Plumbing retained:** content/search **index pipeline**; **checklist / update / Source / Place** bounded Zod + canonical **`src/content/...`** loaders (contract-only); **User / User State / User Checklist Status / Request Submission** stub modules (Epic 1.3); **i18n** + responsive **shell**; **design tokens** + **`Button` / `Card` / `SectionHeader`**; **Sentry/Plausible** stubs + **`logger.ts`**; **IA_SPEC §6.1–6.9** guard + airport redirect; **template shells** for hub/guide/calculator/utility/service-form; **`RoutePlaceholder`** for **`/`** only; **`/start`** = Phase 2 onboarding (steps 1–5 + Step 6 Result Summary, guest blob + outcome preview); **`/dashboard`** = Phase 2 NBA v1 consumer (**`DashboardNextBestAction`**); **PR CI** on **`main`**.
- **`DEVELOPMENT_BREAKDOWN.md`** is **phase-aligned** with the master roadmap (2026-04-02); use it with the **Canonical roadmap phases** section at the top of that file.

## What the next session should do

1. Read **`CURSOR_NEW_CHAT_PROTOCOL.md`**, **`PROJECT_STATE.md`**, **`CURRENT_PHASE.md`**, **`ENGINEERING_ARCHITECTURE.md`**, **`NEXT_ACTIONS.md`**. If the user wants **planning or prioritization**, follow **`CURSOR_PLANNING_PROTOCOL.md`** first.
2. If the user wants a **prompt** for planning or implementation, read **`PROMPT_AUTHORING_PROTOCOL.md`** and keep the prompt **discovery-first** unless implementation of the exact task is already approved.
3. Continue **Phase 2** per `CURRENT_PHASE.md` / `CURRENT_FOCUS.md` / `DEVELOPMENT_BREAKDOWN.md` Gate B — smallest safe slices; keep Phase 1 regression green (`lint` / `test` / `build`).
4. Before prod: close **`OPEN_ITEMS.md`** (legal, governance names, env projects).
5. Before commit/push: run **`npm run lint`**, **`npm run test`**, **`npm run build`** locally.

## Still incomplete / watch

- Replace placeholder research **`.docx`** with a repo-readable export when available (ops, not blocking Phase 1).
