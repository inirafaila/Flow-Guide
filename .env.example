# Optional: Plausible (public site domain in Plausible)
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=flow-guide.example

# Optional: Sentry — use Sentry wizard before prod; DSN must not leak to untrusted builds if policy requires server-only reporting
# SENTRY_DSN=
# NEXT_PUBLIC_SENTRY_DSN=
